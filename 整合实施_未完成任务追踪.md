# testCodeFast → testhub 整合：未完成任务追踪

> 范围：整合实施 M0（环境与依赖）/ M1（迁移适配与映射）/ M2（安全判定 /verdict）/ M4（联调验证与总验收）。（M3 文档未提供，见 M4-R3）
> 用途：记录各阶段**已确认但未闭环**的项，作为后续迭代的输入。每项标注：描述 / 阻塞原因 / 建议处理阶段。
> 约定：✅ 已完成闭环的项不入此表；本表只收「已知未做 / 待补」。

---

## 一、M0 遗留（环境依赖）

| # | 未完成项 | 阻塞/原因 | 建议处理 |
|---|---|---|---|
| M0-R1 | testgen 与 testhub 的 **starlette 版本分化**需长期警惕：testgen 独立 venv 用 starlette 1.6.0，testhub 主环境用 0.50.0（保护 channels/Daphne WebSocket）。两栈靠 sidecar HTTP 隔离，未共享依赖。 | 设计即如此（sidecar 本意） | 升级 testgen 时禁止动 testhub 的 `starlette==0.50.0`；CI 增加对两侧 starlette 版本的断言 |
| M0-R2 | 文档 M0-2 原 `git subtree add` 命令因 testgen-service 在 `work/` 子目录下不可用，已临时改用「方案 B 精炼复制」（不带 git 历史）。 | 时间/结构限制 | 若需保留演进历史，后续走「方案 A 改良」：先在 testCodeFast 把 `work/testgen-service` 抽独立分支再 subtree add |

---

## 二、M1 遗留（迁移适配与映射）

| # | 未完成项 | 阻塞/原因 | 建议处理 |
|---|---|---|---|
| M1-R1 | **ORM 写库未联调**：本机托管 Python 无 Django / MySQL，`apps/testgen_integration` 的用例回流（flow.sync_cases 的 upsert 回调）仅用注入式内存 dict 单测，未在真实 `TestCase` 表落库验证。 | 环境缺 Django+MySQL | 在具备 Django+MySQL 的环境执行 `manage.py migrate` + 跑一次 `/api/testgen/import` 真实回流，确认字段落库与幂等 |
| M1-R2 | M1 的 `views.py` / `urls.py` 仅 `py_compile` 校验语法 + 字段对齐确认，**未在 Django 运行环境启动验证**。 | 环境缺 Django | 随 M1-R1 一并联调 |
| M1-R3 | 未跑 `lint` + `pytest`（AGENTS.md / CODE_STANDARDS §4 要求提交前跑；本仓库无活跃 git 钩子强制）。 | 无强制钩子 | 接入远程 CI 或补 pre-commit 后补跑；当前 M1 单测用 stdlib `unittest`（testgen venv 未装 pytest），未纳入 testhub `pytest.ini` |
| M1-R4 | testgen 测试夹具含**假密码/假 token**（`s3cret` / `S3cret-PW-xyz` / `Tp123456` / `dummy-bearer-token`），源自源仓库 `work/testgen-service` 测试代码。M1 按「testgen 业务零改」未动。 | 源仓库自带；非真实凭证 | 若 `testHub` 公开仓库需脱敏合规，应将这些占位值改为明显 dummy 常量或移出默认测试集 |
| M1-R5 | `TestCase` 真实模型缺 `case_type`/`external_id`/`source`/`suite` 字段（与 M1 文档蓝图不符），已按现实对齐（`test_type`/`tags`/`author` 必填 / `steps`≤1000）。 | 已修正 | 已闭环于 M1 实现；此处仅留痕，提醒后续若 testhub 模型演进需同步 mapping |

---

## 三、M2 遗留（安全判定 /verdict）

| # | 未完成项 | 阻塞/原因 | 建议处理 |
|---|---|---|---|
| M2-R1 | **自动建缺陷的真实生产接线未落地**：`flow.run_security_verdict` 默认调用 `client.create_security_defect`，后者在未配置 `TESTHUB_BASE_URL` 时**主动报错**，要求调用方注入 `create_defect` 回调。真实环境更稳妥的做法是 Django 后端任务里直接 `Defect.objects.create(...)`，而非 testhub 自环 HTTP。 | 缺 Django 运行环境 + 自环 HTTP 不优雅 | 在 testhub 后端 action / Celery task 里注入 `create_defect=Defect.objects.create`（或封装 ORM 写入），并配置 `TESTHUB_BASE_URL`（若走 API） |
| M2-R2 | **越权复测（PRIV_ESC）双身份比对未实现**：testgen `/verdict` 只做单条观测判定；M2 文档要求 testhub 以两个身份各执行一次、把两次观测都发给 `/verdict` 比对。当前仅单条判定就绪，「双身份执行编排」缺位。 | 需 testhub `ui_automation`/`api_testing` 双身份执行能力 | 在 security 用例执行编排中支持双身份 run，聚合两次 facts 再调 `/verdict` |
| M2-R3 | **执行后钩子未挂到真实执行编排**：`flow.run_security_verdict` 已实现，但 testhub 在 security 标记用例执行完成后**真正触发**它的钩子（后端 action / Celery / 执行编排回调）尚未接入。 | 需改动 `ui_automation`/`api_testing` 执行收尾 | 在 security 用例执行成功路径末尾调用 `run_security_verdict(observations)`，注入 `verdict_fn=client.verdict` + `create_defect=ORM写入` |
| M2-R4 | 与 M1-R1 同源：缺陷建单（`Defect` 表）未在 Django+MySQL 真实环境验证（仅注入式单测断言 `create_defect` 被调用、payload 字段正确）。 | 环境缺 Django+MySQL | 随 M2-R1 一并联调，确认 `DefectWriteSerializer` 字段（title/description/severity/priority/source/related_testcase_id/project_id）落库无误 |
| M2-R5 | `/verdict` 未做 AUTH_TOKEN 之外的额外审计日志；仅复用 app 既有 `request_id` 结构化日志。安全判定属敏感操作，建议补充「谁、对哪个 case、判定结果」的留痕。 | 可后续增强 | 在 verdict 端点补充 security audit log（不记录 facts 明文） |

---

## 四、M3 遗留（CI / 部署 / 凭证）

| # | 未完成项 | 阻塞/原因 | 建议处理 |
|---|---|---|---|
| M3-R1 | **secret-scan 在 CI 中对 testgen 测试夹具假密码会告警**：`services/testgen/tests/` 含 `_SECRET_PASSWORD="S3cret-PW-xyz"`、`login_password="Tp123456"` 等明显假值；`scripts/check_secrets.py` 的 `KEY_ASSIGN` 规则判为 `GENERIC_ASSIGN` 并退出码 1，导致 `testgen-gate` 的 secrets 步骤在真实 CI 中失败。`.secrets-allowlist.json` 被 gitignore（M0 排除），CI checkout 无此白名单。 | 既有测试夹具 + allowlist 未入库 | 二选一：① 将 `.secrets-allowlist.json` 改为入库（登记假值行）；② 将夹具值改为明显 dummy 常量（如 `os.environ.get("TEST_PW","dummy")`）。建议随 M1-R4 一并脱敏 |
| M3-R2 | **门禁八环在本机未实跑**：`scripts/gate_all.py`（ruff/format/enum/structure/mypy/bandit/pytest cov≥60）依赖托管 quality env（ruff/mypy/bandit）与 pytest-cov，本机 testgen venv 未装；仅 `py_compile` 校验了脚本语法。 | 无 CI runner + 工具未装 | 接入 GitHub Actions `testgen-gate.yml`（已建）后在 CI 跑通；cov≥60 阈值以 testgen 现有测试集为准 |
| M3-R3 | **webhook 真实事件钩子未联调**：`/api/v1/verify/webhook` 端点与 `WebhookRequest` 就绪，但发布流水线真实部署后调用（含 execute=false 仅生成、幂等键 `wh:...`）未在真实流水线环境验证。 | 缺发布流水线环境 | 在发布流水线「部署完成」步骤接 curl 调用（见 CREDENTIALS.md），确认 202 + 幂等 |
| M3-R4 | **compose/Docker 部署未实跑**：`docker-compose.testgen.yml` + `Dockerfile` + `.dockerignore` 已建，但未在容器环境构建运行验证。 | 本机无 Docker | 在目标机 `docker compose -f services/testgen/docker-compose.testgen.yml up --build` 验证 `/health` 可达、端口不外泄 |

---

## 五、M4 遗留（联调验证与总验收）

| # | 未完成项 | 阻塞/原因 | 建议处理 |
|---|---|---|---|
| M4-R1 | **主链路 HTTP 层未端到端联调**：本机无 Django+MySQL，无法起 testhub :8000，故 M4-1 真实「经 /api/testgen/* 端点」的 HTTP 联调（generate→sync→TestCase 落库→defects 建单）未在运行态验证；仅以 `test_e2e_m4.py`（注入式 + 真实 sidecar）覆盖了背后编排逻辑。 | 环境缺 Django+MySQL | 在具备 Django+MySQL 环境执行 `services/testgen/tests/integration_e2e.sh`，确认端到端 |
| M4-R2 | **门禁双绿未跑**：testgen 八环（ruff/format/enum/structure/mypy/bandit/pytest cov≥60）与 testhub 既有 CI 本机均未执行（testgen venv 未装 pytest，无 CI runner）。 | 无 CI runner + 本机限制 | 接入远程 CI 跑八环 + testhub pytest；当前仅 `py_compile` + stdlib unittest 覆盖 |
| M4-R3 | **M3 阶段未执行**：M4 上游标注「阶段 M0~M3」，但本会话仅做了 M0/M1/M2，M3 文档未提供、范围未确认。M4 作为总验收，其主链路依赖的 M1 端点（/api/testgen/generate、/api/testgen/sync）已具备。若 M3 涉及独立功能（如某类执行编排对接）未做，需在 M3 明确后补。 | M3 文档/范围缺失 | 提供 M3 文档后评估并补齐 |
| M4-R4 | **命名对齐（已闭环留痕）**：文档 M4-1 用 `/api/testgen/import` 与 `/api/testgen/tasks/{id}`，代码落地为 `/api/testgen/sync` 且轮询由 `client.poll()` 直连 testgen（testhub 不另开 tasks 端点）。功能等价，已在 INTEGRATION.md 标注。 | 已设计对齐 | 已闭环；仅留痕 |
| M4-R5 | **现有机能零回归的运行时冒烟未做**：M4-4 要求跑 testhub 文档来源需求分析→用例→执行→报告全流程确认零回归，本机无法起 testhub 故未跑。代码层面已确认 21 app 零改动 + urls 仅追加 1 行，零回归风险低。 | 环境缺 Django | 在 Django 环境补一次冒烟 |

---

## 六、本追踪文档自身的状态

- 生成时间：整合 M2 执行完成后（M0/M1 缺口回填 + M2 缺口追加）。
- 已闭环、不在此表的内容：
  - M0：testgen 迁入 `services/testgen`、独立 venv、探活三项全过、推送 GitHub ✅
  - M1：集成模块 client/mapping/flow/views/urls 生成、35 项自测全过、修复 3 类缺陷、推送 GitHub ✅
  - M2：testgen `/api/v1/verdict` 端点 + `engine/verdict.py`（复用 executor._judge，零逻辑重写）、testhub `client.verdict()` + `flow.run_security_verdict`、47 项自测全过（含真实 sidecar 端到端）✅
  - M3：部署编排 `testgen.service`/compose/`Dockerfile`/`.dockerignore`、独立 CI `testgen-gate.yml`（八环+密钥扫描，不与 testhub CI 合并）、凭证托管 `CREDENTIALS.md`、webhook 发布后自动验证接入 ✅
  - M4：端到端集成测试 `test_e2e_m4.py`（起真实 sidecar，覆盖 M4-1 主链路/幂等、M4-2 安全闭环、M4-3 八类失败路径）✅；HTTP 层联调脚本 `integration_e2e.sh` ✅；运维手册 `INTEGRATION.md` ✅；凭证明文 grep 全假值（无真实明文）✅；可回滚（git 三独立 commit d2b0897/6968f72/1229f73）✅；OpenAPI 增量 diff（仅 +/api/testgen/*，21 app 零改动）✅
- 下一步建议优先级：M2-R1/R3（生产接线）> M1-R1/R2（Django 联调）> M4-R1（HTTP 端到端联调）> M4-R2/M3-R2（门禁双绿）> M3-R1（夹具脱敏，随 M1-R4）> M3-R3/R4（webhook/compose 真实验证）> M0-R2（git 历史，可选）> M1-R4（脱敏，视公开需求）。
