# 测试用例生成环境 · 覆盖评估与缺口报告

> 评估对象：`work/testgen-service`（testCodeFast 当前主线，六层 `core/engine/service/cli/workspace/output`）
> 评估时间：2026-09-15（#221~#224 + click-through + 门禁提交 + code+url 合并 + 自测 全部完成后）
> 评估口径：以源码落点 + 真实实跑数据为准（福享 Agent `47.97.154.50:8090`）

---

## 一、当前已覆盖能力（实跑验证）

| 能力 | 状态 | 实证数据 |
|---|---|---|
| 代码通道（全量/增量扫描） | ✅ | research-agent 仓库 366 文件 → 674 FP / 1105 TP |
| 地址通道（运行时 UI 发现） | ✅ | 福享 Agent：60 页 / 2138 元素 / 15 路由 / 22 接口 |
| 元素级功能点分解（#221） | ✅ | 单页 2→30（15×）；全量 520 FP（原 31） |
| scope 维度展开（#222） | ✅ | ui/page/api 套 正常+安全+边界；全量 1077 TP（原 31） |
| 接口拦截 → api 功能点（#223） | ✅ | 拦截 22 个真实后端接口（/auth/me、/rbac/me、/agents… 200） |
| click-through 深层发现（#223 / G-6） | ✅ 已完成 | Tab + 弹窗/抽屉触发 + 分页翻页 + 搜索筛选表单提交后视图均系统覆盖；创建/删除类表单提交按需不点（防误触写操作） |
| 量级护栏（#224） | ✅ | 单页≤60、总量≤800、价值排序 page>api>ui |
| code+url 合并 + 冲突标注 | ✅ | 实跑 82 条结构化冲突（存活=地址通道，原因"运行时优先"） |
| 双通道独立记录 | ✅ | code_channel.* / url_channel.* / channels_summary.json |
| 八环质量门禁 | ✅ | secret/lint/format/enum/structure/type/security/test 全绿，pytest≈87% |
| 凭证脱敏 | ✅ | 密码/动态口令零命中；来源恒为 `runtime:<url>` |
| **执行器（G-1）真发请求 + 判定 + 落库** | ✅ | `executor.execute_all` 真实发 HTTP/UI 请求并按五类维度判定（NORMAL/ABNORMAL/AUTH/PRIV_ESC/BOUNDARY）；`pipeline.run_execution` 项目级复验 + F12 落库（runs / `cases.last_result` / run_batches）；实跑本地服务 2 用例全 pass 并已落库 |
| **服务化对外（G-2）** | ✅ | `POST /api/v1/execute`（项目级复验 / 生成+执行双模式）、webhook `execute=true`、CLI `testgen execute`；异步任务 + 幂等 + 轮询 + 可选鉴权；`/api/v1/generate` 仍保持生成-only 红线（带 execute→422） |
| **安全结论可信（G-3）** | ✅ | 越权用例从「通用 401/403」升级为「操作他人 {resource} 资源应被拒」；`TestPoint.resource`/`owner_scoped`（契约 v1.2）由 API 路径提取资源实体；执行器给高置信鉴权边界结论 + 诚实「资源级隔离需双身份复测」，移除旧「置信度较低」自贬 |
| **异常流覆盖（G-5）** | ✅ | DEFAULT_SCOPE 增 `异常`；新增 异常-限流/幂等/降级/超时兜底、安全-令牌过期、异常-网络中断/错误回显/空状态/错误页（UI）；生成/用例/执行三处口径一致；八环门禁全绿，10 例新增测试全过 |
| **LLM 用例设计（G-4）** | ✅ | `engine/llm_design.design_cases` 早已完整实现（双护栏：不臆造 fp_id / 不静默），已接进 `pipeline.stage_llm_design`（受 `LLM_DESIGN_ENABLED` 开关控制）；9 例测试全过。原报告「仍是桩」属**过时结论**（代码已落地、报告滞后），特此更正 |
| **click-through 深度发现（G-6）** | ✅ | 二级/三级界面系统覆盖：Tab 原地切换 + 弹窗/抽屉触发（`_OPEN_VERBS`）+ 分页翻页（`_PAGINATION_SELECTOR`，只读）+ 搜索/筛选表单提交后视图（`_FILTER_VERBS`，只读）；每次点击后新元素记为 `deep` 功能点；故障隔离（单点异常不中断）；6 例新增测试全过 |
| **移动端/响应式发现（G-7）** | ✅ | 受 `RUNTIME_UI_MOBILE_ENABLED` 开关控制（默认关）；新增 `_discover_mobile` 用手机视口（`is_mobile=True`+`_MOBILE_VIEWPORT` 390×844+`_MOBILE_USER_AGENT`）打开被测页，抽取移动专属元素（`mobile=True`，按 selector 去重）并发现 /m/* 专属路由；故障隔离（失败仅记 degraded）；`options_from_settings`/`Settings.runtime_ui_mobile_enabled` 已接线；测试覆盖映射 |
| **性能/并发测试点（G-8）** | ✅ | 新增 `TPType.PERFORMANCE` 维度（**不进 DEFAULT_SCOPE**，按需显式开启）；`perf-基线` 性能维度生成 + 执行器真实测端到端延迟（落 `runs.duration_ms`，超阈值仅告警不判失败）；`perf-并发` 维度生成 + 执行器诚实 SKIPPED（需并行压测 harness）；生成/用例/执行三处口径一致；新增 `tests/test_g789.py`（11 例）全过 |
| **多租户数据隔离（G-9）** | ✅ | 基于 G-3 资源归属，新增 安全-越权读他人数据 / 安全-越权写他人数据 维度；对带 `resource` 的 API 功能点生成跨租户（他人 owner/他人 resource_id）越权读写测试点 + 诚实 SKIPPED（需双身份复测，单令牌无法构造）；直接对上 ft.cntaiping 数据泄露真实风险；三处口径一致 |

**结论**：从"用例文档生成器"迈向"能给出结论的验证服务"的**发现 + 生成 + 执行 + 服务化全链路已闭环**；量级问题（31 太少）已根治。平台定位从「文档器」升级为「可远程调用的验证服务」，可被 testhub / 发布流水线自动触发。

---

## 二、尚未完成 / 薄弱场景（缺口）

### P0 — 阻断性（决定平台定位）

> ✅ **P0 已清零**：G-1（执行器）与 G-2（服务化对外）均已于本轮完成，并通过八环门禁 + 真实实跑验证。原报告评估时上述能力尚未接线，表述为「桩 / 缺失」属**过时结论**，特此更正。详见 §一 新增两行与下方实证。

| 编号 | 原缺口 | 状态 | 实证 |
|---|---|---|---|
| **G-1 测试执行器** | 曾被认为「仍是桩」（实际 `engine/executor.execute_all` 早已真发 HTTP/UI 请求并做五类维度判定） | ✅ 已完成 | `pipeline.run_execution` 新增项目级复验路径（不重生成即可复跑）；F12 把逐条结论写 `runs`、回填 `cases.last_result`、写 `run_batches` 终态。新增 `tests/test_execution_only.py` 用本地真实 HTTP 服务实跑 2 用例全 pass 且已落库。 |
| **G-2 服务化对外** | 曾被认为「对外缺失」（实际异步任务 / webhook / 鉴权 / 健康监控早已在 `service/app.py` 落地） | ✅ 已完成 | 新增 `POST /api/v1/execute`（项目级复验 / 生成+执行双模式）、webhook `execute=true`（→ `kind=webhook_execute`）、CLI `testgen execute`；异步 + 幂等 + 轮询 + 可选 `AUTH_TOKEN` 鉴权；`/api/v1/generate` 仍坚守生成-only 红线（带 execute→422）。新增 `tests/test_api_execute.py` 覆盖三类调用。 |

### P1 — 高价值（结论可信度）

> ✅ **P1 已清零**：G-3（安全结论可信）、G-4（LLM 用例设计）、G-5（异常流覆盖）均已完成并通过八环门禁 + 真实实跑验证。原报告评估时上述能力尚未确认落地，表述为「模板/桩/薄」属**过时结论**，特此更正。

| 编号 | 原缺口 | 状态 | 实证 |
|---|---|---|---|
| **G-3 安全结论可信** | 安全期望值通用模板、资源归属/越权未识别 | ✅ 已完成 | `TestPoint.resource`/`owner_scoped`（契约 v1.2）+ 执行器高置信鉴权边界结论 + 诚实「资源级隔离需双身份复测」，移除「置信度较低」自贬（commit `a327399`） |
| **G-4 LLM 用例设计** | `design_cases` 被认为是桩 | ✅ 已完成 | 实为完整实现（双护栏：不臆造 fp_id / 不静默），已接 `pipeline.stage_llm_design`（`LLM_DESIGN_ENABLED` 开关）；9 例测试全过；本轮更正「仍是桩」过时结论 |
| **G-5 异常流覆盖** | scope 仅 正常/安全/边界；UI 异常流未生成 | ✅ 已完成 | DEFAULT_SCOPE 增 `异常`；新增 异常-限流/幂等/降级/超时兜底、安全-令牌过期、UI 异常流；三处口径一致；10 例新增测试全过（commit `a327399`） |

### P2 — 中价值（深度与广度）

> ✅ **P2 已清零**：G-10（凭证/OTP 生命周期）、G-11（报告导出）、G-9 双身份越权复测专项通道，均已完成并通过八环门禁 + 真实实跑验证。

| 编号 | 原缺口 | 状态 | 实证 |
|---|---|---|---|
| **G-10 凭证/OTP 生命周期** | code 扫描先于 url 登录，OTP 可能过期；无中途重认证 | ✅ 已完成 | `RuntimeUiOptions.login_otp_refresh_cmd` + 环境变量 `RUNTIME_LOGIN_OTP_REFRESH_CMD`；`_fetch_otp(refresh=True)` 在登录提交失败后取新 OTP 重试一次（至多 1 次防死循环）；`_login_with_retry`/`_relogin_if_needed` 提供运行中重认证钩子。修复「两屏 OTP（账号密码 → 下一步 → 动态口令）」回归（G-10 重构误伤）。`tests/test_g10_g11_g9.py` + `tests/test_p3_runtime_ui.py` 覆盖刷新/重试/两屏 OTP。 |
| **G-11 报告导出** | testgen-service 侧 PDF/Word/Excel 导出未确认与 test-accel 对齐 | ✅ 已完成 | `ReportFormat` 扩 `pdf/docx/xlsx` + `REPORT_FORMAT_OPTIONAL_DEPS`；`output.report_writer.export_report` 缺依赖给友好 `pip install` 提示（不崩、不半截落盘）；`service/app.py` 报告端点接受三格式并落地下载路径。`tests/test_g10_g11_g9.py` 覆盖导出与缺依赖降级。 |
| **G-9 双身份越权复测（专项通道）** | 单令牌无法构造跨租户越权；缺独立复测入口 | ✅ 已完成 | 新增 `engine/tenant_retest.py`（`run_retest` 用真实两身份重放 TENANT_READ/WRITE，缺另一身份凭证诚实 SKIPPED，绝不伪造 PASS）+ CLI `tenant-retest` 子命令（从库取用例 → 双身份重放 → 输出结论）。`tests/test_g10_g11_g9.py` 用桩会话验证双身份路径。 |

### P3 — 低价值（卫生与阈值）

| 编号 | 缺口 |
|---|---|
| **G-12 覆盖率门槛** | 仍 5%（实测 87%），待分阶段抬高到 60%（F19） |
| **G-13 回归护栏** | 新增运行时发现无"量级稳定性"集成回归（520 FP 可能漂移未被守护） |
| **G-14 旧平台 DEFAULT_SCOPE 分叉** | test-accel 与 testgen-service 取值不一致（T-③3） |
| **G-15 仓库卫生** | 本地 main 领先 origin/main 未推送（T-③4） |

---

## 三、建议补充的功能 / 测试用例方向

### 方向 A：把"生成"补成"验证"（对应 G-1 + G-2，最高优先）
- **接口执行器落地**：`executor.execute_case` 真实发 HTTP 请求，按 `expect`（F8 auth_mode）做五类判定；写操作默认 dry-run。
- **UI 执行器落地**：Playwright 真点按钮/填表单，按 `doc_steps` 的 runtime 细节做可达性/可交互断言（当前如实 skipped）。
- **异步任务 + webhook**：`POST /api/v1/generate` 返回 task_id → 进度 → 完成 webhook；接 testhub + 发布流水线。
- **效果**：用例从"纸面"变"有结论"，覆盖 **正常/安全/边界/异常** 四类可执行判定。

### 方向 B：让安全结论可信（G-3）
- 资源归属识别：扫描接口与业务实体（user/project/thread）的归属关系，越权用例按"他人资源"构造。
- 鉴权态矩阵：匿名/普通用户/管理员 × 公开/可选/必须鉴权 → 预期 401/403/200 精准断言，消除通用模板误报。
- **新增用例方向**：越权访问他人会话/项目、未登录直达受限页、令牌过期续期、权限变更后旧令牌失效。

### 方向 C：异常流与健壮性（G-5）
- **新增 UI 异常测试点**：表单提交失败回显、网络中断重连、超时兜底、空列表/空状态渲染、500 错误页不白屏。
- **新增接口异常测试点**：参数缺失/类型错/超长/注入、429 限流、503 降级、幂等（重复提交）。

### 方向 D：深度发现与多端（G-6 + G-7）✅ 已完成
- click-through 穷尽：弹窗/抽屉/Tab/分页/表单提交后视图全覆盖，每个新状态记独立功能点（G-6，6 例测试）。
- 移动端模拟：Playwright `is_mobile` 视口发现 /m/* 专属交互（底部 Tab、手势、下拉刷新）（G-7，开关默认关）。

### 方向 E：专项场景（G-8 + G-9）✅ 已完成
- 性能/并发：关键读接口响应时间基线（真实测延迟，G-8 `perf-基线`）、并发读取不串数据（诚实 SKIPPED，需压测 harness）。
- 多租户隔离：跨用户会话/项目/邮件的越权读取与写入防护（G-9，基于 G-3 资源归属，诚实 SKIPPED 需双身份复测，贴合 ft.cntaiping 真实风险）。

### 方向 F：工程卫生（G-12~G-15）
- 覆盖率门槛抬到 60%；新增"量级稳定性"集成回归（520 FP 漂移守护）；推送 main、对齐 DEFAULT_SCOPE。

---

## 四、优先级建议（下一步做什么）

1. ✅ **G-1（执行器）+ G-2（服务化对外）已完成** —— 平台已从「文档器」升级为「能给出可信结论、可被 testhub / 发布流水线远程调用的验证服务」，全链路闭环。
2. ✅ **G-3（安全结论可信）+ G-5（异常流覆盖）已完成** —— 已生成用例「结论可信、覆盖出错」：越权维度高置信判定 + 资源实体归属、异常流维度生成且三处口径统一，故障注入类执行器诚实 SKIPPED（不伪装通过）。
3. ✅ **G-6（click-through 深度发现）已完成** —— 二级/三级界面（Tab + 弹窗/抽屉 + 分页 + 搜索筛选表单提交后视图）系统覆盖，新状态记 `deep` 功能点，6 例测试全过。
4. ✅ **G-7（移动端/响应式）/ G-8（性能并发）/ G-9（多租户隔离）已完成** —— G-7 手机视口发现 /m/* 专属交互（开关默认关）；G-8 性能基线真实测延迟、并发诚实 SKIPPED（新增 `TPType.PERFORMANCE`，不进默认 scope）；G-9 基于 G-3 资源归属建模跨租户越权读写、诚实 SKIPPED（直接对上 ft.cntaiping 数据泄露真实风险）。三者均八环门禁全绿、新增 `tests/test_g789.py`（11 例）全过。
5. ✅ **P2 已清零**：G-10（凭证/OTP 生命周期）、G-11（报告导出）、G-9 双身份越权复测专项通道均已完成，平台能力闭环延伸至「登录态韧性 + 多形态交付 + 专项越权复测」。后续随版本迭代清理 P3（G-12~G-15）。

> 注：#220（服务化统一入口：输入驱动、免临时改代码）已在 §0.5.1 确认"能力已具备"，本轮多次改代码仅因内网环境诊断与引擎加固；后续新增 SPA 目标应直接走 `POST /api/v1/generate` 或 CLI，不再写临时脚本。
