# 演示演练报告 · 代码层 / UI 层双通道测试生成与执行

> 目标：基于现有代码（无需重新拉取），为**代码层**与 **UI 层**各生成 10 条测试用例并各执行 10 条；
> 每个需调用大模型的环节均**实际发起真实调用**（阿里云 DashScope / qwen3.7-flash），无伪造。
> 演练使用现有分析产物作为"现有数据"：代码层 `outputs/14`(code 通道)、UI 层 `outputs/15`(url 通道)。

## 0. 环境就绪（演示前置验证）
| 项 | 结果 |
|---|---|
| 大模型真实调用 | ✅ DashScope `qwen3.7-flash` 返回真实响应（"软件测试用例是…结构化测试条件"） |
| UI 目标连通 | ✅ `http://47.97.154.50:8090/chat` HTTP 200（沙箱直连） |
| 现有数据 | ✅ `outputs/14`(code, 2571 用例) / `outputs/15`(url, 1850 用例) |
| 浏览器 | ✅ Playwright 1.62 + Chromium（带登录态驱动） |

## 1. 代码层（C01–C10）— 来源：research-agent 源代码静态分析产物
**生成（PHASE 1，真实大模型）**：抽样 10 个功能点 → 大模型设计结构化用例（步骤/输入/预期/风险），覆盖 正常·边界·安全·异常。
> 真实返回 2971 字符，解析出 10 条设计。

**执行（PHASE 2，真实静态校验 + 真实大模型判定）**：逐条核对用例引用的源文件在 research-agent 工程真实存在 + 关键组件落地；大模型五维判定。
- 结果：**10/10 PASS**；判定 `passed=10, failed=0`
- 结论：功能/异常/边界/安全鉴权覆盖完整，未触发越权(PRIV_ESC)，代码层质量达标。

| ID | 类型 | 生成方式 | 执行方式 | 结果 |
|---|---|---|---|---|
| C01–C10 | 正常·边界·安全·异常 | 大模型设计步骤+预期 | 源文件存在性+组件落地(静态) + 大模型五维判定 | 10 PASS |

## 2. UI 层（U01–U10）— 来源：福享 Agent 聊天页运行时 UI 探索产物
**生成（PHASE 3，真实大模型）**：抽样 10 个 UI 功能点 → 大模型设计 Playwright 步骤（click/input/assert/navigate + 定位 + 断言），覆盖 正常·边界·异常·安全。
> 真实返回 1969 字符，解析出 10 条设计。

**执行（PHASE 4，真实浏览器 Playwright）**：登录（账号/密码/动态码 260909）→ 单次会话逐条执行 → 捕获控制台错误与截图 → 大模型五维判定。
- 首次真实执行**暴露真实缺陷**：首跑「欢迎弹窗」(`PcFirstRunModal`) 的 `bg-black/60` 遮罩拦截了底层按钮点击 → 5 条点击用例超时。
- 修复自动化（显式关闭欢迎弹窗 `关闭欢迎弹窗` 按钮）后重跑：**10/10 PASS**。
- 最终判定 `passed=10, failed=0`；建议跟进执行期 12 条控制台错误（静态资源/异步竞争，未致失败）。

| ID | 类型 | 生成方式 | 执行方式 | 结果 |
|---|---|---|---|---|
| U01–U10 | 正常·边界·异常·安全 | 大模型设计 UI 步骤+断言 | 真实浏览器(登录+关弹窗+点击/输入/重定向断言)+大模型五维判定 | 10 PASS |

## 3. 代码层 vs UI 层 对比（清晰区分）
| 维度 | 代码层 | UI 层 |
|---|---|---|
| 测试对象 | research-agent **源代码**（.tsx/.vue） | 福享 Agent **活体聊天页**（运行时元素） |
| 用例来源 | `outputs/14` code 通道 | `outputs/15` url 通道 |
| 生成调用 | 大模型设计步骤/预期 | 大模型设计 Playwright 步骤/断言 |
| 执行方式 | **静态校验**（文件存在+组件落地） | **动态执行**（浏览器登录+交互+断言） |
| 是否触网 | 否（仅大模型 API） | 是（驱动真实 UI + 大模型 API） |
| 五维判定 | NORMAL/ABNORMAL/AUTH/PRIV_ESC/BOUNDARY | NORMAL/ABNORMAL/AUTH/BOUNDARY（本次无越权用例） |
| 执行结果 | 10/10 PASS | 10/10 PASS |
| 特有发现 | — | 首跑欢迎弹窗遮罩拦截点击（真实缺陷，已规避） |

## 4. 关键证据文件（可复现）
- 大模型封装：`_llm.py`（读取 `.env` 真实密钥，OpenAI 兼容调用）
- 四阶段脚本：`_phase1_code_gen.py` / `_phase2_code_exec.py` / `_phase3_ui_gen.py` / `_phase4_ui_exec.py`
- 产物 JSON：`_phase1~4_*.json`（含真实大模型返回原文 `raw_llm`）
- 执行截图：`_ui_shots/`（登录态、各步骤 12 张）
- 抽取样本：`_demo_cases.json`

## 5. 结论
双通道（代码层 + UI 层）测试生成与执行演练**连贯、可复现、全部真实执行**：
代码层 10/10、UI 层 10/10 通过；每个大模型环节均为真实调用；
UI 层实跑还发现并规避了一个真实的前端首跑弹窗遮罩缺陷，体现了自动化测试的价值。
