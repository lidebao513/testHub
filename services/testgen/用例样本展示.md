# 测试用例样本展示（真实产出）

> 数据来源：`work/testgen-service/data/testgen.db`　项目：research-agent（id=1）
> 全库用例总数：**1293**　契约版本：1.0

本文件由 `work/testgen-service/tools/dump_case_samples.py` 直接从数据库导出，
未经手工修改，用于核对「代码 → 功能点 → 测试点 → 用例」链路的真实产出。

## 执行层与维度总览

| 执行层 | 用例类型 | 条数 |
|---|---|---|
| 接口 | api | 667 |
| 接口 | e2e | 485 |
| UI | e2e | 141 |

| 维度 | 子维度 | 条数 |
|---|---|---|
| 正常 | 业务函数-逻辑可用 | 485 |
| 边界 | 边界-参数缺失/非法 | 235 |
| 安全 | 安全-鉴权缺失 | 171 |
| 正常 | 正常-可用性 | 171 |
| 正常 | 页面/路由可达 | 102 |
| 异常 | 异常-资源不存在 | 64 |
| 正常 | 交互元素可用 | 39 |
| 安全 | 安全-越权 | 26 |

| 覆盖角色 | 条数 | 含义 |
|---|---|---|
| primary | 1293 | UI 优先／接口为该能力唯一覆盖方式 |

## A. 接口层 · HTTP 接口（共 171 条，下列 3 条）

> 代码里的路由装饰器（`@router.get("/x")` 等）→ 用 HTTP 请求直接打接口。

#### `TP-6dc02c17` [正常] research-agent-source · 创建/api/sql/translate

| 字段 | 值 |
|---|---|
| 编号 | `TP-6dc02c17` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-aa2d2477` → `TP-6dc02c17` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql/translate，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql/translate 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：返回 2xx，资源创建成功，响应体包含新建对象标识

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-6dc02c17",
  "fp_id": "FP-aa2d2477",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql/translate",
  "func": null,
  "dimension": "正常-可用性",
  "expect": "返回 2xx，资源创建成功，响应体包含新建对象标识",
  "coverage_role": "primary"
}
```

#### `TP-973b72ea` [正常] research-agent-source · 创建/api/sql_explanation/retry

| 字段 | 值 |
|---|---|
| 编号 | `TP-973b72ea` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-2b43392b` → `TP-973b72ea` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql_explanation/retry，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql_explanation/retry 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：返回 2xx，资源创建成功，响应体包含新建对象标识

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-973b72ea",
  "fp_id": "FP-2b43392b",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql_explanation/retry",
  "func": null,
  "dimension": "正常-可用性",
  "expect": "返回 2xx，资源创建成功，响应体包含新建对象标识",
  "coverage_role": "primary"
}
```

#### `TP-1559d985` [正常] research-agent-source · 查询列表/api/audit/selfcheck

| 字段 | 值 |
|---|---|
| 编号 | `TP-1559d985` |
| 模块 | research-agent-source |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-156a8149` → `TP-1559d985` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：GET /api/audit/selfcheck，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 GET /api/audit/selfcheck 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：返回 2xx，响应体为列表结构，分页字段可用

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-1559d985",
  "fp_id": "FP-156a8149",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "GET",
  "path": "/api/audit/selfcheck",
  "func": null,
  "dimension": "正常-可用性",
  "expect": "返回 2xx，响应体为列表结构，分页字段可用",
  "coverage_role": "primary"
}
```

## B. 接口层 · 后端业务函数（共 485 条，下列 2 条）

> 不是 HTTP 接口，是被调用到的业务函数／工具脚本，归在接口层用函数级断言验证。

#### `TP-01fd5ed1` [正常] agents · list_contributions

| 字段 | 值 |
|---|---|
| 编号 | `TP-01fd5ed1` |
| 模块 | agents |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-41d39e22` → `TP-01fd5ed1` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 准备调用上下文：list_contributions，来源文件 backend/research-agent-source/agents/contributions_api.py
3. [执行] 调用函数 list_contributions，捕获返回值与异常
4. [断言] 校验：返回值符合语义，异常分支被正确捕获
   - 预期：函数返回符合语义的结果，异常分支被正确捕获

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "business",
  "tp_id": "TP-01fd5ed1",
  "fp_id": "FP-41d39e22",
  "source": "backend/research-agent-source/agents/contributions_api.py",
  "file": "backend/research-agent-source/agents/contributions_api.py",
  "method": "FUNC",
  "path": "list_contributions",
  "func": "list_contributions",
  "dimension": "业务函数-逻辑可用",
  "expect": "函数返回符合语义的结果，异常分支被正确捕获",
  "coverage_role": "primary"
}
```

#### `TP-82e2dbe4` [正常] agents · submit_contribution

| 字段 | 值 |
|---|---|
| 编号 | `TP-82e2dbe4` |
| 模块 | agents |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-e4a4f108` → `TP-82e2dbe4` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 准备调用上下文：submit_contribution，来源文件 backend/research-agent-source/agents/contributions_api.py
3. [执行] 调用函数 submit_contribution，捕获返回值与异常
4. [断言] 校验：返回值符合语义，异常分支被正确捕获
   - 预期：函数返回符合语义的结果，异常分支被正确捕获

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "business",
  "tp_id": "TP-82e2dbe4",
  "fp_id": "FP-e4a4f108",
  "source": "backend/research-agent-source/agents/contributions_api.py",
  "file": "backend/research-agent-source/agents/contributions_api.py",
  "method": "FUNC",
  "path": "submit_contribution",
  "func": "submit_contribution",
  "dimension": "业务函数-逻辑可用",
  "expect": "函数返回符合语义的结果，异常分支被正确捕获",
  "coverage_role": "primary"
}
```

## C. UI 层 · 页面可达（共 102 条，下列 3 条）

> 前端路由（React `<Route path="/pc/chat">` / `path: '/x'`）→ 打开页面看能否渲染。

#### `TP-d5384a1c` [正常] src · 页面/

| 字段 | 值 |
|---|---|
| 编号 | `TP-d5384a1c` |
| 模块 | src |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-5ef4b1b1` → `TP-d5384a1c` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 打开页面：/，来源文件 frontend/app/src/App.tsx
3. [执行] 访问 /，等待首屏渲染完成并收集控制台错误
4. [断言] 校验：页面正常渲染（无白屏／无控制台报错），关键元素可见
   - 预期：页面/路由可正常渲染，无白屏或控制台报错

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "page",
  "tp_id": "TP-d5384a1c",
  "fp_id": "FP-5ef4b1b1",
  "source": "frontend/app/src/App.tsx",
  "file": "frontend/app/src/App.tsx",
  "method": "PAGE",
  "path": "/",
  "func": null,
  "dimension": "页面/路由可达",
  "expect": "页面/路由可正常渲染，无白屏或控制台报错",
  "coverage_role": "primary"
}
```

#### `TP-7b3551c3` [正常] src · 页面/chat

| 字段 | 值 |
|---|---|
| 编号 | `TP-7b3551c3` |
| 模块 | src |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-4c57a26f` → `TP-7b3551c3` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 打开页面：/chat，来源文件 frontend/app/src/App.tsx
3. [执行] 访问 /chat，等待首屏渲染完成并收集控制台错误
4. [断言] 校验：页面正常渲染（无白屏／无控制台报错），关键元素可见
   - 预期：页面/路由可正常渲染，无白屏或控制台报错

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "page",
  "tp_id": "TP-7b3551c3",
  "fp_id": "FP-4c57a26f",
  "source": "frontend/app/src/App.tsx",
  "file": "frontend/app/src/App.tsx",
  "method": "PAGE",
  "path": "/chat",
  "func": null,
  "dimension": "页面/路由可达",
  "expect": "页面/路由可正常渲染，无白屏或控制台报错",
  "coverage_role": "primary"
}
```

#### `TP-a52b384f` [正常] src · 页面/chat/:id

| 字段 | 值 |
|---|---|
| 编号 | `TP-a52b384f` |
| 模块 | src |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-89e57531` → `TP-a52b384f` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 打开页面：/chat/:id，来源文件 frontend/app/src/App.tsx
3. [执行] 访问 /chat/:id，等待首屏渲染完成并收集控制台错误
4. [断言] 校验：页面正常渲染（无白屏／无控制台报错），关键元素可见
   - 预期：页面/路由可正常渲染，无白屏或控制台报错

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "page",
  "tp_id": "TP-a52b384f",
  "fp_id": "FP-89e57531",
  "source": "frontend/app/src/App.tsx",
  "file": "frontend/app/src/App.tsx",
  "method": "PAGE",
  "path": "/chat/:id",
  "func": null,
  "dimension": "页面/路由可达",
  "expect": "页面/路由可正常渲染，无白屏或控制台报错",
  "coverage_role": "primary"
}
```

## D. UI 层 · 交互组件（共 39 条，下列 2 条）

> 带交互钩子的前端组件（onClick / onChange / onSubmit / <button> 等）→ 点击输入看响应。

#### `TP-9fb1a3f6` [正常] components · 交互组件TasksTab.vue

| 字段 | 值 |
|---|---|
| 编号 | `TP-9fb1a3f6` |
| 模块 | components |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-893db95d` → `TP-9fb1a3f6` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 定位交互点：TasksTab.vue，来源文件 backend/research-agent-source/audit_ui/src/components/TasksTab.vue
3. [执行] 触发 TasksTab.vue 的交互（点击／输入），观察界面响应
4. [断言] 校验：交互后界面按预期变化，无报错提示或状态残留
   - 预期：交互元素可用，点击后产生预期状态变化

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "ui",
  "tp_id": "TP-9fb1a3f6",
  "fp_id": "FP-893db95d",
  "source": "backend/research-agent-source/audit_ui/src/components/TasksTab.vue",
  "file": "backend/research-agent-source/audit_ui/src/components/TasksTab.vue",
  "method": "UI",
  "path": "TasksTab.vue",
  "func": null,
  "dimension": "交互元素可用",
  "expect": "交互元素可用，点击后产生预期状态变化",
  "coverage_role": "primary"
}
```

#### `TP-a3113fd1` [正常] chat · 交互组件Composer.tsx

| 字段 | 值 |
|---|---|
| 编号 | `TP-a3113fd1` |
| 模块 | chat |
| 类型（维度） | 正常 |
| 优先级 | P2 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | UI（ui_probe） |
| 来源功能点 | `FP-0ad44239` → `TP-a3113fd1` |
| 用例 ctype | e2e |

**前置条件**：被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）

**步骤**

1. [前置] 被测服务已启动且前端可访问；已准备可用浏览器（Playwright）与测试账号（如需登录）
2. [准备] 定位交互点：Composer.tsx，来源文件 frontend/app/src/components/chat/Composer.tsx
3. [执行] 触发 Composer.tsx 的交互（点击／输入），观察界面响应
4. [断言] 校验：交互后界面按预期变化，无报错提示或状态残留
   - 预期：交互元素可用，点击后产生预期状态变化

**机器步（执行器只读第 1 步）**

```json
{
  "action": "ui_probe",
  "layer": "UI",
  "kind": "ui",
  "tp_id": "TP-a3113fd1",
  "fp_id": "FP-0ad44239",
  "source": "frontend/app/src/components/chat/Composer.tsx",
  "file": "frontend/app/src/components/chat/Composer.tsx",
  "method": "UI",
  "path": "Composer.tsx",
  "func": null,
  "dimension": "交互元素可用",
  "expect": "交互元素可用，点击后产生预期状态变化",
  "coverage_role": "primary"
}
```

## E. 安全 · 鉴权缺失（共 171 条，下列 2 条）

> 未携带或携带无效凭证访问接口，应被拒绝。

#### `TP-939f15fd` [安全] research-agent-source · 创建/api/sql/translate

| 字段 | 值 |
|---|---|
| 编号 | `TP-939f15fd` |
| 模块 | research-agent-source |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-aa2d2477` → `TP-939f15fd` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql/translate，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql/translate 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-939f15fd",
  "fp_id": "FP-aa2d2477",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql/translate",
  "func": null,
  "dimension": "安全-鉴权缺失",
  "expect": "未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）",
  "coverage_role": "primary"
}
```

#### `TP-afbb9c52` [安全] research-agent-source · 创建/api/sql_explanation/retry

| 字段 | 值 |
|---|---|
| 编号 | `TP-afbb9c52` |
| 模块 | research-agent-source |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-2b43392b` → `TP-afbb9c52` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql_explanation/retry，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql_explanation/retry 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-afbb9c52",
  "fp_id": "FP-2b43392b",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql_explanation/retry",
  "func": null,
  "dimension": "安全-鉴权缺失",
  "expect": "未携带或携带无效凭证时返回 401/403，且不泄露资源内容（越权访问同样被拒）",
  "coverage_role": "primary"
}
```

## F. 安全 · 越权访问（共 26 条，下列 2 条）

> 带路径参数的写操作（PUT/PATCH/DELETE），用非属主身份访问。

#### `TP-0746ac16` [安全] research-agent-source · 更新/api/v1/memories/{mem_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-0746ac16` |
| 模块 | research-agent-source |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-1628fe87` → `TP-0746ac16` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：PATCH /api/v1/memories/{mem_id}，来源文件 backend/research-agent-source/server.py
3. [执行] 发送 PATCH /api/v1/memories/{mem_id} 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：以他人身份/越权凭证操作该资源 → 403，且不产生越权修改

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-0746ac16",
  "fp_id": "FP-1628fe87",
  "source": "backend/research-agent-source/server.py",
  "file": "backend/research-agent-source/server.py",
  "method": "PATCH",
  "path": "/api/v1/memories/{mem_id}",
  "func": null,
  "dimension": "安全-越权",
  "expect": "以他人身份/越权凭证操作该资源 → 403，且不产生越权修改",
  "coverage_role": "primary"
}
```

#### `TP-5ef36b61` [安全] research-agent-source · 删除/api/v1/memories/{mem_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-5ef36b61` |
| 模块 | research-agent-source |
| 类型（维度） | 安全 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-b88befdd` → `TP-5ef36b61` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：DELETE /api/v1/memories/{mem_id}，来源文件 backend/research-agent-source/server.py
3. [执行] 发送 DELETE /api/v1/memories/{mem_id} 请求并捕获响应
4. [断言] 校验：无凭证/越权访问被拒绝（401/403），且不泄露资源内容
   - 预期：以他人身份/越权凭证操作该资源 → 403，且不产生越权修改

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-5ef36b61",
  "fp_id": "FP-b88befdd",
  "source": "backend/research-agent-source/server.py",
  "file": "backend/research-agent-source/server.py",
  "method": "DELETE",
  "path": "/api/v1/memories/{mem_id}",
  "func": null,
  "dimension": "安全-越权",
  "expect": "以他人身份/越权凭证操作该资源 → 403，且不产生越权修改",
  "coverage_role": "primary"
}
```

## G. 异常（共 64 条，下列 2 条）

> 仅对有路径参数的接口展开：资源不存在／状态非法。

#### `TP-dfe11231` [异常] research-agent-source · 查询详情/api/v1/pipeline/items/{item_id}

| 字段 | 值 |
|---|---|
| 编号 | `TP-dfe11231` |
| 模块 | research-agent-source |
| 类型（维度） | 异常 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-28c6bd3a` → `TP-dfe11231` |
| 用例 ctype | api |

**前置条件**：被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式

**步骤**

1. [前置] 被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式
2. [准备] 构造请求：GET /api/v1/pipeline/items/{item_id}，来源文件 backend/research-agent-source/pipeline_api.py
3. [执行] 发送 GET /api/v1/pipeline/items/{item_id} 请求并捕获响应
4. [断言] 校验：接口对非法/边界输入返回预期错误（4xx/5xx），且不产生未捕获异常或数据损坏
   - 预期：返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-dfe11231",
  "fp_id": "FP-28c6bd3a",
  "source": "backend/research-agent-source/pipeline_api.py",
  "file": "backend/research-agent-source/pipeline_api.py",
  "method": "GET",
  "path": "/api/v1/pipeline/items/{item_id}",
  "func": null,
  "dimension": "异常-资源不存在",
  "expect": "返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常",
  "coverage_role": "primary"
}
```

#### `TP-97f9384b` [异常] research-agent-source · 创建/api/v1/pipeline/items/{item_id}/events

| 字段 | 值 |
|---|---|
| 编号 | `TP-97f9384b` |
| 模块 | research-agent-source |
| 类型（维度） | 异常 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-bb379964` → `TP-97f9384b` |
| 用例 ctype | api |

**前置条件**：被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式

**步骤**

1. [前置] 被测服务已启动且 base_url 可达；已明确合法/非法输入的构造方式
2. [准备] 构造请求：POST /api/v1/pipeline/items/{item_id}/events，来源文件 backend/research-agent-source/pipeline_api.py
3. [执行] 发送 POST /api/v1/pipeline/items/{item_id}/events 请求并捕获响应
4. [断言] 校验：接口对非法/边界输入返回预期错误（4xx/5xx），且不产生未捕获异常或数据损坏
   - 预期：返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-97f9384b",
  "fp_id": "FP-bb379964",
  "source": "backend/research-agent-source/pipeline_api.py",
  "file": "backend/research-agent-source/pipeline_api.py",
  "method": "POST",
  "path": "/api/v1/pipeline/items/{item_id}/events",
  "func": null,
  "dimension": "异常-资源不存在",
  "expect": "返回 4xx（资源不存在 404 / 状态非法 409），响应体为结构化错误信息，服务不抛未捕获异常",
  "coverage_role": "primary"
}
```

## H. 边界（共 235 条，下列 2 条）

> 参数缺失／非法：所有 API 路由各一条，写操作再补一条凑对。

#### `TP-82887f00` [边界] research-agent-source · 创建/api/sql/translate

| 字段 | 值 |
|---|---|
| 编号 | `TP-82887f00` |
| 模块 | research-agent-source |
| 类型（维度） | 边界 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-aa2d2477` → `TP-82887f00` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql/translate，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql/translate 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：参数缺失或越界时返回 400/422，校验信息明确指出非法字段

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-82887f00",
  "fp_id": "FP-aa2d2477",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql/translate",
  "func": null,
  "dimension": "边界-参数缺失/非法",
  "expect": "参数缺失或越界时返回 400/422，校验信息明确指出非法字段",
  "coverage_role": "primary"
}
```

#### `TP-1b62bf1f` [边界] research-agent-source · 创建/api/sql_explanation/retry

| 字段 | 值 |
|---|---|
| 编号 | `TP-1b62bf1f` |
| 模块 | research-agent-source |
| 类型（维度） | 边界 |
| 优先级 | P1 |
| 测试类型 | 全量 |
| 覆盖角色 | primary |
| 期望执行层 | 接口（http_probe） |
| 来源功能点 | `FP-2b43392b` → `TP-1b62bf1f` |
| 用例 ctype | api |

**前置条件**：被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）

**步骤**

1. [前置] 被测服务已启动，base_url 可达；涉及鉴权接口需持有有效 token（或测试账号可匿名访问）
2. [准备] 构造请求：POST /api/sql_explanation/retry，来源文件 backend/research-agent-source/audit_pipeline.py
3. [执行] 发送 POST /api/sql_explanation/retry 请求并捕获响应
4. [断言] 校验：响应状态码符合预期，关键业务字段完整
   - 预期：参数缺失或越界时返回 400/422，校验信息明确指出非法字段

**机器步（执行器只读第 1 步）**

```json
{
  "action": "http_probe",
  "layer": "接口",
  "kind": "api",
  "tp_id": "TP-1b62bf1f",
  "fp_id": "FP-2b43392b",
  "source": "backend/research-agent-source/audit_pipeline.py",
  "file": "backend/research-agent-source/audit_pipeline.py",
  "method": "POST",
  "path": "/api/sql_explanation/retry",
  "func": null,
  "dimension": "边界-参数缺失/非法",
  "expect": "参数缺失或越界时返回 400/422，校验信息明确指出非法字段",
  "coverage_role": "primary"
}
```

## I. 增量命中：更新标签（共 0 条，下列 0 条）

> 本次 diff（base..target）改动到的文件所涉测试点，标为「更新」。

（无数据）

## J. 接口层 · 补充用例（对应能力已有 UI 覆盖）（共 0 条，下列 0 条）

> UI 优先策略下，对应模块已有 UI 覆盖的接口层用例被标记为 `supplement`：UI 是优先验证层，接口仅作补充。

（无数据）

## 产物文件清单

| 文件 | 内容 |
|---|---|
| `outputs/1/test_cases.json` | 全部用例（机器可读，含机器步） |
| `outputs/1/TEST_CASES.md` | 全部用例（人类可读清单） |
| `outputs/1/test_points.json` | 全部测试点（含证据引用） |
| `outputs/1/summary.json` | 本次运行汇总（计数／标签／追溯体检） |
