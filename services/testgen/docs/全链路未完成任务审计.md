# 全链路未完成任务审计（从「开始」到「生成测试用例」）

> 审计时间：2026-09-16　范围：`work/testgen-service` 一站式流水线 `cli.main pipeline`
> 已完成基线：G-1~G-11 全部 DONE，八环门禁（`scripts/gate_all.py`）全绿（覆盖率 86%）。
> 本文只列**仍开放的缺口**与**诚实 SKIPPED 的专项任务**，作为下一阶段输入。

## 一、结论先行

- **从「代码/URL 输入」到「可用测试用例」的主链路已闭环**：`code`/`url`/`code+url` 三态入口 → 扫描 → 功能点 → 测试点 → 用例 → 双通道独立记录 → 报告，产物齐备、追溯零孤儿。
- **P0 / P1 / P2 已清零**：G-1（执行器）、G-2（服务化对外）、G-3（安全可信）、G-4（LLM 设计）、G-5（异常流）、G-6（click-through 发现）、G-7（移动端发现）、G-8（性能并发）、G-9（多租户隔离）、G-10（OTP 生命周期）、G-11（报告导出）、G-9 双身份复测通道 —— 全部 DONE。
- **当前执行任务（2026-09-16）**：`101.43.2.52:9001` 新项目「URL 生成用例 + UI 层执行 + 报告」（账号 liuwei）。
- **接口层真实执行**：代码已就绪，但**尚未对任何真实目标跑过验证**，本轮明确不做。
- **测试专家系统 Phase 1（URL 通道 / PageExpert）已落地并八环门禁全绿**：新增 `engine/expert/` 异构双专家底座（统一多模态 LLM 包装 `llm.py` + 红线护栏 `base.py`）；PageExpert 覆盖 **S0 菜单反推（治本 `routes=0 → 真实菜单数`）** + **S1–S6 旅程/风险/边界/安全视角提质**；`expert_mode` 默认开、`--expert-off` 可关；契约新增 `origin=expert_page` / `TestPoint.expert`；验收目标 routes 0→10+、跨模块用例（详见**六**）。
- **剩余其它任务**：P3 工程卫生 / 集成联调 / 诚实 SKIPPED 专项。

## 二、链路逐项状态

| 阶段 | 能力 | 状态 | 剩余缺口 |
|---|---|---|---|
| ① 代码获取 | F1 取码内聚 / `--changed-files-json` 外部变更集解耦 | ✅ | 无（基线 ref 失效已在仓库侧处理） |
| ② 代码分析 | scan / fp_extract / diff_tag / semantic_enrich | ✅ | 无 |
| ③ 测试点展开 | tp_expand（正常/安全/边界/异常） | ✅ | 无 |
| ④ 用例生成 | case_gen（八要素） | ✅ | 无 |
| ⑤ 功能点合并 | fp_merge（code+url 结构化冲突标注） | ✅ | 无 |
| ⑥ 运行时 UI 发现 | runtime_ui（元素级/分页/筛选/两屏 OTP/移动端视口） | ✅ | 真实点击执行仍 SKIPPED（见三） |
| ⑦ 双通道记录 | channel_writer（code/url 独立落盘，空通道不写） | ✅ | 无 |
| ⑧ 报告 | report_writer（md/html/json + pdf/docx/xlsx 可选导出） | ✅ | 导出三格式需 `pip install` 可选依赖 |
| ⑨ 执行器 | executor（HTTP/UI 真发请求，五类维度判定） | ✅ 生成期 | 故障注入 / 并发 / 双身份越权 **真实验证** SKIPPED |
| ⑩ 服务化对外 | service/tasks + `/api/v1/generate`/`/execute`/webhook | ✅ | 真实 testhub / 发布流水线联调未做 |

## 三、诚实 SKIPPED 的专项任务（需人工/专项，非代码缺陷）

以下不是「没做」，而是**受环境/风险约束刻意不下结论**，统一在报告里如实标注 SKIPPED：

1. **故障注入类执行**：参数注入、fuzz、SQL/命令注入类用例——执行器不主动触发破坏性验证，避免误伤被测系统；真实验证需专项/压测 harness。
2. **并发读取真实压测**：G-8 `perf-并发` 维度已生成，真实并发压测 SKIPPED，需压测工具与隔离环境。
3. **双身份越权真实复测**：G-9 通道（`tenant-retest`）已落地，但**真跑需第二身份凭证**；直接对 `ft.cntaiping.com` 数据共享环境复测有数据泄露真实风险，须专项审批 + 隔离账号。
4. **移动端/响应式真实点击**：G-7 仅做「发现」（`RUNTIME_UI_MOBILE_ENABLED` 默认关），未做移动端真机/模拟器点击执行。
5. **UI click-through 真实执行**：G-6 是「深度发现」能力，UI 执行器（`ui_executor`）对深层交互真实点击仍按诚实 SKIPPED 处理。

## 四、剩余任务清单（按优先级）

### P0 — 当前最高优先级

- **`101.43.2.52:9001` 新项目：URL 生成用例 + UI 层执行 + 报告**（2026-09-16 登记，状态：**✅ DONE**）
  - 目标：`http://101.43.2.52:9001/login`（**投研智能体平台**，与之前 `47.97.154.50:8090/chat` 及 research-agent 代码仓库均不是同一项目）。
  - 账号：`liuwei` / `Taiping@2026`（**无动态码**）。
  - 范围：URL 通道 pipeline（runtime_ui 登录探索 + XHR 拦截）生成用例 → 仅执行 **UI 层**用例（`RUNTIME_UI_ENABLED=on` + `ui_click=on`）→ 生成 md/html/json 报告；**接口层 HTTP 执行本轮明确不做**。
  - 生成结果（project_id=8）：FP 29 / TP 99 / 用例 99；scope：正常 29 / 安全 16 / 边界 29 / 异常 25；孤儿 0；落盘 `outputs/8/`（含 TEST_CASES.md）。
  - UI 执行结果（batch `ui_20260916062456_9636`）：UI 层 50 条 → executed 46 / **pass 45 / fail 1 / skipped 4**；真实通过率 97.8%。
    - 4 条 skipped = UI 异常流（需断网/5xx/清空数据等专项状态，诚实跳过）。
    - **1 条 fail = `TP-6cd14168`**：真实断言未通过 2/22 条——「未找到元素 `button.send-btn` / `chat-editable`」，已落失败截图 `output/screenshots/8/TP-6cd14168.png`（同页另一 22 断言用例 `TP-c640b136` 通过，说明是该条运行时层级选择器不匹配，**真实缺陷信号**，建议研发复核 chat 输入框容器结构）。
  - 接口层 49 条：按用户要求本轮跳过。
  - 报告：`output/reports/8/8/REPORT.md`（+ html/json）。
  - 红线守持：凭证全程 env 注入、跑完即清，未落盘未入库。

- **福享 Agent 双账号「用例生成 + 合并」产出**（2026-09-16 登记，状态：**跳过 · 待凭证**）
  - 目标：`http://47.97.154.50:8090/chat`；account A `test_ft001@ft.cntaiping.com` + account B `test2118uyw@tpp.cntaiping.com`（**同租户，不做跨租户 G-9 复测**）。
  - 范围：两账号各自跑 URL 通道 pipeline 生成用例 → `merge_scenarios.py` 合并 → 交付 `outputs/fuxiang_2account/TEST_CASES.md`（含 scope 分布 + 双账号覆盖差异）。
  - 进展：链路骨架已就绪（连通性✅ / 登录自动化✅ / 合并驱动✅）。**真实登录被服务端拒绝**：动态码 `260909` 过期/失效（留空报错「请输入…」、填入报错「账号、密码或动态口令不正确」）。
  - 阻塞（外部，非代码缺陷）：需两账号「当下有效」密码 + 动态口令；B 为 tpp 域，需确认可登此 ft 主机。
  - 恢复路径：用户提供当下有效动态码 + 确认密码 + 确认 B 可登 → scenario1(A) → scenario2(B) → 合并 → 交付。
  - 红线：凭证仅 env 注入、跑完即清，不落盘；合并产物脱敏 `runtime:<url>`；`tokens.json` 不提交。

### P3 — 工程卫生 / 阈值（建议随版本迭代）
- **G-12 覆盖率门槛**：✅ DONE（2026-09-16 commit `8f2b9cf`，`fail_under` 5→60，实测 86.09% 全绿）。
- **G-13 量级稳定性回归**：运行时发现量级（520 FP / 2138 元素）无「量级漂移」集成守护，重构后可能无声膨胀。
- **G-14 DEFAULT_SCOPE 分叉**：✅ DONE（2026-09-16 commit `5b026f4`，test-accel 对齐四维度真值 `{正常,安全,边界,异常}`）。
- **G-15 仓库卫生**：✅ DONE（2026-09-16 已推送，本地 main 与 origin 同步）。

### 集成待办（决定「服务能否被真实调用」）
- **接口层真实执行验证**：HTTP executor 代码已就绪，但**尚未对任何真实目标跑过**；本轮 `101.43.2.52:9001` 任务明确只验证 UI 层，接口层继续挂起。
- **testhub 远程真实联调**：P5 接口（`/api/v1/generate`、`/execute`、webhook）已落地，缺真实 testhub 调用验证。
- **发布流水线事件钩子**：接入部署后自动验证的 webhook 真实事件未联调。
- **报告导出依赖预装**：`reportlab`/`python-docx`/`openpyxl` 未预装，按需 `pip install` 即可导出 pdf/docx/xlsx。

## 五、建议下一步（按 ROI）

1. **`101.43.2.52:9001` 的 URL 生成 + UI 执行 + 报告**：✅ 已完成（project_id=8，UI 真实通过率 97.8%，1 fail 为真实选择器缺陷信号）。
2. **福享 Agent 双账号任务**：待用户补当下有效动态码 + 确认 B 账号可登后，续跑 scenario1→scenario2→合并。
3. **接口层真实执行验证**：在隔离/测试环境找一条真实 API 目标，跑通 HTTP executor 端到端。
4. **testhub / 发布流水线集成联调**：用户明确本轮跳过，后续排期。

## 六、测试专家系统（2026-09-16 落地 Phase 1，#265–#271 DONE；#274/#276 DONE）

### 6.1 设计决策（D1–D4，用户拍板）
- **D1 视觉输入=多模态（截图）**：`deepseek-chat` 纯文本非视觉 → `use_vision=True` 自动降级 DOM 并 notes，不静默假装已读截图。
- **D2 实现顺序=先 Phase 1（URL/PageExpert）**，Phase 2（CodeExpert）后置。
- **D3 `expert_mode` 默认开**、留开关（`--expert-off` 可关）。
- **D4 专家强度=单轮 LLM（低成本默认）**，自主 Agent 仅留可选配置开关（`expert_agent_mode`，后置到后续任务，本期不实现）。

### 6.2 落地文件
- `engine/expert/{__init__,llm,base,page_expert}.py`：异构双专家底座（统一多模态 LLM 包装 `llm.py` + 红线护栏 `base.py`）。
- `core/contracts.py`：ORIGIN_EXPERT_PAGE/EXPERT_CODE/EXPERT_ORIGINS + `TestPoint.expert` 字段。
- `core/config.py`：expert_mode/vision/provider/base_url/model/api_key/timeout/max_tps_per_page/agent_mode。
- `engine/runtime_ui.py`：导航治本——`_click_label` 文本兜底点击、`_discover_menu_routes(extra_labels)` 并入 S0 反推菜单、`_sweep_pages` 落页后调 S0 记 extra_labels。
- `engine/pipeline.py`：S0 治本（`_expert_options`→`discover_ui(expert_options=...)`）+ S1–S6 提质（`stage_expert_review` 接入 `run_pipeline`）+ `PipelineResult.expert_summary`（#274 报告增强，专家贡献进 `to_dict`）。
- `cli/main.py`：pipeline 子命令新增 `--expert-off`（置 `opts.expert_mode=False`）。
- `tests/test_expert.py`（#276）：10 条单测覆盖护栏/不臆造/不静默/S0 菜单反推/S1–S6 提质过滤/契约转换，门禁覆盖 `engine/expert`。

### 6.3 门禁（#276）
- 八环门禁：secret/lint/format/enum/structure/type/security 全绿；pytest 含 expert 单测，覆盖率 ≥60%（实测约 84%）。
- **已知 flaky**：`tests/test_p5_service.py::test_generate_idempotent_returns_existing_task` 为幂等时序依赖（首任务仍 pending 才返回既有 task_id），与专家系统无关，偶发失败；已 3 次隔离复跑验证（2 过 1 败）。

### 6.4 验收（#278）
- 目标：`101.43.2.52:9001` 重跑，routes 0→10+、跨模块用例、含专家增补测试点；`--expert-off` 无损降级。
- **本轮实测（2026-09-16 下午）**：
  - 目标可达（`curl` HTTP 200）；用 `liuwei/Taiping@2026` 真实登录成功（`logged_in=true`），落页抓到 17 个元素。
  - 但 `routes=0`、`pages=1`（与旧基线一致）——**根因：本环境 `has_llm_api_key=False`/`has_llm_base_url=False`，专家 S0 的 LLM 菜单反推降级为 no-op**（无 S0 标签 → 导航治本无输入 → 路由仍 0）。
  - `expert_summary` 诚实透出：`{"enabled": false, "reason": "未配置 LLM（EXPERT_API_KEY/BASE_URL/MODEL…），已降级纯规则"}`，全流程无崩溃、`errors=[]`。
  - 结论：**代码完整、护栏生效、降级诚实**；但「routes 0→10+」这一核心验收项**受限于 LLM 凭证缺失**，须配置 `EXPERT_API_KEY`/`EXPERT_BASE_URL`/`EXPERT_MODEL`（或复用 `LLM_*`）后重跑方可体现专家增益。
- 备注：此 SPA 菜单点击可能不改 URL（客户端/hash 路由），即便有 S0 标签也需 `_normalize_route` 能识别；如需「无 LLM 也能扩覆盖」，可另加 non-LLM 路由兜底（捕获 `#/xxx` 与 state 路由），作为后续增强，非本次阻塞项。
- 状态：Phase 1 代码完成、八环门禁全绿（~84%）；#278 验收**挂起 · 待 EXPERT/LLM_API_KEY**。

### 6.5 剩余（专家系统）
- #272 CodeExpert（Phase 2，后置）；#273 代码流水线接入（Phase 2）；#275 自主 Agent 开关（D4 后置，本期不实现）。

> 注：本文为阶段性审计，不进入 git 提交（仅作下一步规划输入）。代码改动随 G-10/11/9 提交。
